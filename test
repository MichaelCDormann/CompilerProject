int test(int a, int b)
{
    return a*5-b/2;
}

void main(void)
{
    int x;
    int y;
    int z;
    int q;
    float w[10];

    x = 1;
    y = 0;
    z = x + y;
    q = (z+5)/100;

    if(x + y/z >= q) {
        while( 5 + 17 == z/2) {
            q = y/x + 7;
            x = test(q, x);
        }
    } else {
        x = w[(5+x)/y + z*20] + 1;
    }
}